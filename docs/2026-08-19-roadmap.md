# Roadmap — 2026-08-19

Driven by `graph-tech-lead`. Ruled on by `graph-senior-dev` (correctness, executed
counterexamples). Field-tested by `ef-manager` (user's seat). Goal: `docs/THE-GOAL.md`.

**Status: DISCUSSED. Both agents have ruled. Nothing is started.**
One item needs Steven's decision because dev and Steven disagree — flagged ⚖ in Phase 4.

> **The product change both agents converged on, in dev's words:** APG should stop being *"a code
> query API with overlays"* and become *"a durable, provenance-bearing repository memory map."*
> **The first feature of that product is not more edges. It is deleting every edge that cannot
> explain why it exists.**

---

## What decides priority

Measured constraints from `docs/2026-08-19-does-this-earn-its-keep.md`. Not re-litigated here.

1. **Discovery is the unmeasured, unclaimed task class.** Every study putting grep at 100%
   presupposes the agent knows the symbol.
2. **Adoption is the largest failure mode** — 58% zero tool calls; 64.6% of sessions never
   invoking. Bigger than any retrieval effect.
3. **The foundation rests on an admission rule that requires no evidence.**
4. **Do not compete on symbol lookup.** At ceiling for grep.

⚠ Steven's bar, which vetoes: *"without producing specific slop or something that has low
value."*

---

## Phase 0 — Stop measuring with a broken ruler ✅ DONE (`873afdc`, `14fb8c9`)

Both A/B harnesses read one `turn.completed` out of N. **One-sided**: under-counts whichever arm
took more turns. Every number that harness produced is suspect.

⚠ **And my fix was wrong, which is the part worth keeping.** It called a growing series
"cumulative" and took the last value — inference from shape, inside the instrument built to stop
inference from shape. dev executed it. **Every non-decreasing series is ambiguous from values
alone**; only a decrease proves anything. Usage semantics is a property of the HOST, so an adapter
now DECLARES it, a declaration the data contradicts is refused, and the collector reports its own
coverage instead of silently skipping turns.

⏳ **Remaining:** establish `usageSemantics` per harness against a provider-reported total on a
frozen transcript. Until then every multi-turn total refuses. **That is correct and it blocks the
efficacy re-run.**

---

## Phase 1 — Make the foundation honest

### The measurement, stated the way it survives review

~2,370 doc→code `MENTIONS` edges over 73 documents on APG; **83.5% have an all-lowercase-word
target**.

⛔ **That is a property of the ADMISSION RULE, not a correctness label.** A doc can genuinely
refer to `read`. My first write-up said "76% wrong" and both agents corrected it independently —
wrong number *and* wrong characterisation. What is established from source: `mentions.js` admits
every regex word collision, takes the **first** node for an ambiguous label, and records **line
0**.

⭐ **One repo could not show the shape.** `ef-manager` ran it on echoes: **63.1%** — twenty points
apart, same extractor. The rate tracks the **language's naming convention**: JavaScript names
functions `exists`, `count`, `list`, `read`, colliding with English head-on; C++ leans CamelCase
and `WorldBuffer` survives contact with prose.
⇒ **No global threshold or hand-tuned stop-word list can be right on both repos.** It must be
derived per-repo from the extracted namespace.

⭐ **The ranking is inverted.** The noisiest term carries 60 edges; the genuinely doc-referenced
symbols — `ensureFresh` 9, `openDb` 7, `clampToBudget` 5, `renderCompact` 4 — carry the fewest.
**Frequency and value are inversely correlated.** "Most-mentioned symbols" is a noise generator;
truncating a mentions list by count drops the signal first.

⚠ `ef-manager`'s scope, which is the more useful fact: *"it cannot have cost me, because I never
consumed it."* In days of intensive field use the doc layer has had **zero consumers**.

### The ruling: DELETE and rederive

dev's ruling, accepted. Not demote, not rank.
- Confidence is ignored or inconsistently applied by degree/ranking consumers.
- Demoting still asserts a relationship exists when only a token collision does.
- **60 wrong edges to `files` distort community, breadth and document ranking *before* ranking
  runs.** You cannot rank your way out.
- Rejected-candidate diagnostics belong in index **telemetry** (`candidates`,
  `rejected_ambiguous`, `rejected_prose_shape`, `admitted_by_rule`), not in graph edges.
  **Edge count is not capability. Removing false data increases capability.**

⛔ **The migration must be deletion-sensitive.** A rebuild purges all `extractor='mentions'`
edges; an incremental doc reindex deletes that document's prior derived edges before rederiving.
`INSERT OR IGNORE` alone preserves the poison forever.

### The invariant, in dev's words

> **A stored doc→symbol edge must carry a recoverable source span and a deterministic resolution
> path to exactly one code node.**

Build a label → **all candidates** map. **Never repeat today's `if (!symbolMap.has(label)) first
wins`.** An ambiguous label emits no edge unless local evidence disambiguates it.

**Admission rules, strongest first:**

1. **Exact Markdown/path link → an indexed repository file.** Emits a dedicated Document→File
   relation (`LINKS_TO`), **not** a symbol `MENTIONS`. ⭐ Highest-value foundation edge, and the
   one that directly serves "where is the design doc".
2. **Qualified code reference resolving uniquely** — `Ns::Type::method`, `Type.method`,
   `obj->method`. Not "strip to the last word and take the first match".
3. **Invocation/type-shaped token resolving uniquely**, in inline code or a language fence —
   `graphWhereis(...)`, `SomeType`, `snake_case_symbol`.
4. **Path-scoped symbol reference** — an exact file and a symbol declared there **in the same
   local span** (link, sentence, list item, fenced example). Whole-document co-occurrence is too
   weak.

⛔ **Backticks alone are NOT sufficient.** Docs backtick `read`, `file`, `query`, and fences hold
shell, JSON and output.
⛔ **Do not own a stopword dictionary** — dev: *"it will become the next unexplained authority."*
⛔ **A bare prose exact-name collision emits nothing, even for a unique symbol.** Uniqueness says
only one target exists; it does not prove the author referred to it.

**Provenance must reach the reader, not just the database:** real `source_line`, `provenance:
INFERRED` (the occurrence is observed; the identity mapping is inferred), `extractor` naming the
rule, confidence fixed **by rule, never by symbol popularity**.
⚠ `documents_mentioning` currently labels the whole field `observed`. That collapse is wrong and
`tests/unit/query/observed-doc-mentions.test.js` enshrines it — both get rewritten.
⛔ **A mention is not causality.** "This decision came from this doc" is a different relation and
a different authority class. It starts as a **curated** overlay link, never inferred from a
mention.

### Acceptance gate

Stratified frozen hand-labelled sample, including negative-control docs using the same word in
ordinary prose. **Precision floor ≥0.95 per admission rule, not aggregate.** Recall disclosed as
a floor. **A rule below the floor is deleted, not rescued by ranking.** `ef-manager` reproduces
independently, on **both** repos, before it is called done.

---

## Phase 2 — Discovery: ALIVE, and the target moved

⭐ **The kill-switch did not fire.** I asked `ef-manager` whether discovery is real or a story
around one anecdote. Their answer is the strongest evidence in this document:

> They hit Steven's failure themselves, twice, and solved it by **hand-building the artifact
> THE-GOAL describes** — `MEMORY.md`, ~120 one-line pointers to documents, maintained by hand
> across compactions, entirely outside our tool, *"because nothing else answers 'what do I know
> that I have forgotten I know'."* And twice this week they needed a specific doc, knew roughly
> what it was about, and got there with `git grep`.

⚠ **Two corrections that change the design:**

1. **Steven's case and `ef-manager`'s case are different problems.** Steven's agent forgot a doc
   **existed** ("what is here"). `ef-manager` knows it exists and cannot **locate** it ("where is
   the thing I half-remember"). A design serving only one will feel like a miss to whoever has
   the other. Both are recorded in THE-GOAL now.
2. ⛔ **Our competitor on discovery is not grep — it is `ls docs/`.** It finds what you do not
   know to search for, costs nothing, and needs no index. On 73 docs, `ls docs/ && head -3` is a
   real bar. ⇒ **Aim the eval at *topic* → doc where the filename does not contain the topic.**
   "Can it find game-design.md" is a test `ls` also passes.

Partly shipped (`5a3b3d9`): multi-word tokenization, and `kind:"all"` reserves page space. That
took **three attempts**. `ef-manager` has two probes designed to find a fourth mistake: a query
whose only hit is a doc whose **filename contains no query word**, and a query with **zero**
legitimate doc answers — *"a reserved slot is a strong incentive to promote something."*

---

## Phase 3 — Adoption

**3a. Task-shaped skills.** `blast-radius` + `safe-to-delete` shipped (`fc30004`). Nine of
thirteen remaining skills operate our tool rather than doing a job.

**3b. ⭐ Hook-injected pointers — Steven's idea, and the strongest adoption lever available.**

*Can we do it today?* **Yes.** We ship a `SessionStart` hook, Claude Code supports `PostToolUse`,
and `graph_explain_diff` already takes `files[]` and returns one-hop callers grouped by file plus
LAYERS/RISK/TESTS. Nothing is wired.

*Why it beats the alternatives:* **a hook does not require the agent to reach.** That answers the
58%/64.6% figures directly instead of trying to persuade an agent that is routing correctly.

⛔ **Highest slop risk in this document.** Our own measured finding: behaviour changes only when a
field **CONTRADICTS** the agent's confidence, never when it adds data. "Here are the callers" on
every edit is a per-edit token cost that agents learn to ignore — and a frequent signal later
disproved teaches them to ignore it permanently.

⇒ **Fire rarely, only on contradiction.** Candidates: callers in files you have not opened this
session; a signature change with callers in TUs the compile DB does not cover; deleting a symbol
that has callers; editing a file an open task marks as another lane's.

**Exit criterion: a measured fire rate.** Fire on more than a small fraction of edits and it is
slop by definition, however clever.
⏳ Placement (Claude Code hook vs `aify-wrapper` contract) is Steven's call. The content is ours
either way, and the content is the hard part.

**3c. Cut the default listing.** A **listing** decision; everything stays callable.

`ef-manager`'s usage across the whole arc — **5 of 17 verbs ever called**: `graph_health` (every
round, *"highest-value call in the set"*), `graph_whereis`, `graph_search`, `graph_packet`,
`graph_impact` (once, found a bug).

⭐ **The datum they said to act on is not the zero counts — it is where they went instead.** Three
times they had a real question, had the full verb list in front of them, and **went to raw
sqlite**. Each produced a finding. *"Presence did not steer me wrong; it steered me NOWHERE."*

- **Keep:** `graph_health`, `graph_search`, `graph_whereis`, `graph_packet`, `graph_impact`.
- **Drop from the default listing:** `graph_dashboard`, `graph_index`, `graph_explain_diff`,
  `graph_digest` — *"three of them are things a human runs, not an agent mid-task."*
- ⛔ **`ef-manager` explicitly refuses to advise** on `graph_trace`, `graph_consequences`,
  `graph_pull`, `graph_explore`, `graph_callers`, `code_intel_references`,
  `code_intel_hierarchy`: *"zero calls is evidence I was not doing the work they serve. Do NOT
  let me be the reason those get cut — a drop decision on my numbers alone would be the
  consumer-enumeration mistake again."* Those go to dev, who has been writing code.

dev's proposal: don't copy codegraph's "1" as a magic number, but accept the mechanism, and A/B a
**map-first** surface via the existing `APG_MCP_TOOLS` allowlist — `graph_search`, `graph_pull`,
`graph_packet`, `graph_health`, `graph_index`, `graph_dashboard`. **Measure mis-picks and task
completion, not schema token count.**

---

## Phase 4 — The refactor ⚖ STEVEN'S CALL: dev says drop it

> **Still open, carried to `docs/2026-08-20-open-decisions.md` with three other questions that are
> preferences rather than work.**


Steven: *"include completing refactor."* dev, unprompted, put it top of the drop list:

> *"`packet.js` being 1453 lines is not itself a user defect. The previous '~710' target was an
> invented global authority. Split only when a specific ownership/test/change is being blocked;
> never restore 'no file over N' as a goal."*

**My read:** dev is right that "no file over N" is an invented authority — that is exactly how
`067e3ad` came to be titled "TARGET MET" while fourteen files exceeded it. But there is a real
defect underneath: `packet.js` at 1453 carries the packet-list guarantee that took **ten
versions** to make honest, and that difficulty was partly the file.

**Proposal, if Steven wants it kept:** reframe from a line target to a **blocked-work** target —
split only where a named guarantee or test is hard to hold, `packet.js` first, and **the
completion claim names its population or is not made.** If Steven prefers dev's version, Phase 4
is deleted and the refactor is closed as "scoped work, done; global target retired as invented."

---

## Phase 5 — Cross-layer edges, after Phase 1 ✅ EDGE LIFECYCLE DONE (`83c9540`)

> **2026-08-20.** The deliverable dev named — the lifecycle ledger — shipped as
> `mcp/stdio/storage/edge-classes.js`, and it is executable rather than written down:
> `ownedEdgesPredicate(id)` returns the SQL a producer must use, and no producer restates it.
> **Eight data-loss defects the same day were missing entries in one column of that ledger** — every
> one a destructive operation that could not answer "which edges are mine to delete?".
> The LLM-inferred edges remain explicitly NOT DONE and not wanted (see the ⛔ below).


Verified borrows: **tier-scoped merge** keyed `(source_file, tier)` so re-extracting code cannot
delete the overlay (graphify `97656fa`); **content-fingerprinted verdicts** that self-flag *"code
changed — re-verify"*; **warn → persist → retry** for dropped edges; **deterministic gating** so
expensive LLM refresh runs only when cheap signals fire.

⚠ **No doc→symbol linking pattern exists to copy** — graphify *documents* one and the code does
not exist.
⛔ **No LLM-inferred decision or governance edges in the authority graph.** dev: attractive slop
unless explicitly curated and reviewable.

**New, from dev, and I had not named it: EDGE LIFECYCLE OWNERSHIP.** Every derived cross-layer
relation needs a producer, population, admission rule, provenance, freshness trigger, deletion
trigger and consumer policy. *"Without that ledger, file rename / doc deletion / overlay drift
leaves truthful-at-creation edges stale, and the foundation becomes wrong again without any
extractor defect."*

---

## Phase 6 — Two things the field asked for

**6a. ⭐ A census verb.** `ef-manager`'s one unmet want, and not a feature request dressed as
feedback: they hand-wrote `SELECT type, count(*) FROM nodes GROUP BY type` in **three separate
rounds and it produced a finding every time** — the four dead declaration types, the
67%-unreachable figure, echoes' 183 `Symbol` + 1 `BuildTest` that whereis silently cannot return.
*"`graph_health` gives me nodes=4624 edges=15788 — two numbers that have never once told me
anything actionable — while the distribution behind them has been the most productive thing I
have run."*

**6b. Stale-process refusal.** ⛔ **Three of `ef-manager`'s last four rounds opened blocked on a
stale MCP process.** And a new finding: **one process serves both repos, so a stale process
poisons every repo it answers for** — the staleness field is scoped to the repo you ask about,
the defect is scoped to the process. *"Right now the only actor who can fix it is the one who
cannot see it."*
⇒ Server-side mtime check on its own loaded files at request time; **refuse rather than warn**;
or an explicit `graph_restart`.
⚠ Related, from their own miss: a raw sqlite reader gets **no** snapshot warning. Two reads
minutes apart returned 0 and 2,369 edges — they read mid-refresh and nearly reported "cannot
reproduce". Direct DB reading has been their highest-yield instrument and it has no
read-consistency signal.

---

## What we are NOT doing

- **Variable/constant extraction** — parked, `docs/2026-08-19-variable-extraction-parked.md`.
- **Selection-receipt wiring** — dev: *pause it*. The codec is approved and dormant; it grants no
  authority and does not serve the discovery goal. Finish it when a real cross-agent audit needs
  it, not because the foundation work made it technically ready.
- **Chasing the attestation competitor.** `codebase-memory-mcp` entering the space is not a
  reason to build their roadmap.
- **New verbs** until the discovery journey cannot be expressed without one. *(6a is the one
  candidate, and it is earned by three findings.)*
- **Dashboard/community polish over dirty edges** — visualisation amplifies bad foundations.
- **Lookup optimisation** — grep wins when the symbol is known.

---

## How we test it — checkpoints in the plan, from `ef-manager`

1. ⭐ **Build identity is checkpoint zero, not optional.** Three of their last four rounds opened
   stale. Any acceptance step not starting with `startedAt` is measuring a ghost.
2. **The boundary walk, both directions.** What does this now PROMISE, can the thing it points at
   keep it — then backward to everything that describes it.
3. **Enumerate by DEFECT CLASS, not code pattern.** The stated-count-over-an-undeduped-list
   defect appeared three times in three unrelated mechanisms; a grep for the previous fix would
   have found none of them. Ask *"where else does something state a number derived from a list or
   a traversal"*, and check identity, not resemblance.
4. ⭐ **Cross-repo before acceptance.** This round is the argument: the same measurement differs by
   twenty points for reasons intrinsic to the language. Anything with a threshold, stop-word list
   or scoring weight runs on both, and **the acceptance criterion is that the DIFFERENCE is
   explainable** — not that both pass.
5. ⭐ **When a fix is scoped, the claim must carry the scope.** `ef-manager` on `067e3ad`: *"the
   cleanest example of a defect I would not have caught by testing. The two files really were
   split. The sentence is what failed."* And their sharper half: **a wrong memory entry is worse
   than a wrong commit message, because it actively suppresses the re-audit that would catch it.
   Correct the memory in the same commit, or the retraction never reaches where the claim is
   doing damage.**

## How we will know it failed

- Phase 1 raises precision and cannot report recall → **the defect moved**.
- Phase 1 ships a threshold tuned on one repo → **it is wrong on the other by construction**.
- Phase 3b fires on a large share of edits → **slop, cut it**.
- Any phase ships a claim dev falsifies with an executed counterexample → **withdrawn, not
  narrowed**.
- Any completion claim without a denominator → **repeating `067e3ad`**.
