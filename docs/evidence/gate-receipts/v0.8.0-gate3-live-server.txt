v0.8.0 TAG GATE ITEM 3 — the REAL SERVER over JSON-RPC, not the module
run at            : 2026-09-06T00:17:14.825Z
server exit code  : 0

[RESULT]    graph_health grants absenceAuthority=true : false   (must be false)
[RESULT]    health denial reason                      : collection_partial
[CONTROL+]  graph_callers("graphCapabilities") returned callers : true
[CONTROL]   absence path RESOLVED the symbol (not NO MATCH) : true
[RESULT]    graph_callers absence says NOT exhaustive : true

The positive control is what makes the absence a measured result rather than a broken query:
a verb that returned nothing for every symbol would produce the same "NOT exhaustive" line.

--- graph_health (excerpt) ---
    "absenceAuthority": false,
    "nextAction": "graph_collect_code_intel({ scope: \"all\" }) to finish coverage; per symbol, read evidence.exhaustive on code_intel_references rather than inferring from this summary",
    "note": "orientationUsable and absenceAuthority are independent. `trust` measures how completely extraction resolved its own references; it does NOT mean any edge was compiler-checked."
      "scripts/gate-absence-refusal.mjs"
    "coverage": {
      "note": "DEGRADED results (definition_only / no_index_entry) are NOT evidence of no callers — the index could not answer, which is a statement about the index, not about the code. Only `clean` counts as an observed absence. Never read `total` as \"symbols with no callers\"."
      "note": "19 CALLS edge(s) are unverifiable BY CONSTRUCTION and are excluded from the coverage denominator. An edge nobody could have verified is not an edge that failed verification. Including them would cap the achievable percentage below 100 and make its movement uninterpretable."

--- graph_callers, absence path ---
NO CALLERS from "tests/self-review" in 969 indexed files (not the whole repository)
TRUST: absence is from the heuristic graph and is NOT exhaustive — for a trustworthy "no callers" check use code_intel_references (live clangd, per-symbol evidence), or verify with rg. LSP SCOPE: the newest code-intel collection is typescript, which processed 73 of 627 eligible files; anything outside it is heuristic only. NOT MODELLED: a call through a computed key — table[name](), obj[k]() — is invisible to the heuristic graph. This states what the analysis cannot see, NOT that this symbol is affected.
NOT COVERED: scripts/gate-absence-refusal.mjs (untracked) — uncommitted, so not indexed. Commit or graph_index({force:true}) before treating this as absent.
