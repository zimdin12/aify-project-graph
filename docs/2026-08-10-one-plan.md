# The plan — v0.6.0 and what comes after

**Built 2026-08-10 from:** findings that survived triage by an agent who did not
report them · `docs/2026-08-10-state-of-the-service.md` (the register) ·
`docs/superpowers/specs/2026-08-10-contradiction-surface-design.md` ·
`docs/code-intel-v2-next-plan.md` · `docs/v0.3-hardening-plan.md` · the
reference-borrow shortlist.

**Team:** this project (me), review, hermes session, the field test (adjudicator),
the field fleet (gated). Steven has handed over the wheel and asked that tool decisions be
made by the agents who use the tools. Owners below are decisions, not proposals.

---

## THE GOAL, and the one number that judges it

> when it becomes actually useful and would give us gains in knowledge and token
> usage — make our agents better at their work and/or consume less tokens.

**Nothing in this plan matters more than section 1.** Everything below it is a defect
in a tool that competent agents are currently not reaching for.

---

## 1 — ★★ WHY COMPETENT AGENTS DO NOT REACH FOR IT  ·  owner: this project

This ranked nowhere in the register's action section. the field test caught that and the
catch was correct: I applied the register's ranking criterion ("by evidence, not by
appeal") to the FIXES and not to the FINDINGS. A register that ranks only what it
knows how to fix will always rank the fixable above the important.

It now has two independent instances, which is more evidence than any other item here.

**Instance 1 — the field fleet.** Asked to name a single time a graph verb changed what
they did: **zero**. Then tested their own excuse (unreachability), found the graph
*was* reachable and trusted, and retracted it themselves.

⛔ **INSTANCE 2 IS WITHDRAWN (2026-08-11). It was never evidence.**

It read: *"the field fleet again, different week, unprompted. Their lanes spent the day on
bounded census, reachability proofs and call-site enumeration — the graph's exact use
case — using `git cat-file`, controlled regex with per-form positive controls, and a
clangd they refused on a degraded banner. Three void instruments caught at the point of
measurement. **They offered this as reassurance that the gate had cost us little.**"*

★★★ **Those lanes were operating under MY gate.** They did not route around the graph —
**they were forbidden it.** That is not evidence of non-adoption; it is evidence of
compliance with a prohibition I set.

★ **And the sentence naming the gate is in the same paragraph.** I wrote down the
constraint and then read the behaviour as a preference, three lines apart — **a
constraint read as a choice**, the exact defect this document is about, committed in the
finding ranked above every other item, on evidence I described as "two independent
instances."

⇒ Caught in the field, aiming at something else (the audit-transcript scope), in the
message where they were standing down. **Six corrections from the adjudicator today and
none of us checked whether my own gate had contaminated my own headline.**

⚠ **THE WITHDRAWAL KILLS THE INFERENCE, NOT THE HYPOTHESIS** (the field fleet, pre-empting the
overshoot):

```
LICENSED       the gate was in force ⇒ a zero from those lanes is UNINFORMATIVE
NOT LICENSED   ⇒ therefore the lanes WOULD have used it
```

⇒ **The correct post-withdrawal state is UNMEASURED**, and unmeasured is not a weaker
version of either answer. §1 must not acquire a sentence like *"adoption would have been
higher."* A withdrawal that flatters neither party is stable; one that quietly flatters
the tool gets re-refuted the first time real data arrives.

### Instance 1 — checked against an immutable record, not memory

the field fleet warned me not to take *"the original zero predates the gate"* from their
recollection or mine — the same class of claim they had just got wrong. Correct, and the
first check failed: **the zero and the gate are first recorded in the same commit
(`d323941`, 2026-08-10)**, so committed docs cannot separate them.

✅ **Resolved from source history instead.** `consequences.js` carries two field reports —
*"the field fleet, Sand Castle, 2026-08-04→05: `graph_consequences` on
`sim/fields/UnifiedFluidWriteback.cpp` returned `tests_adjacent []`"* — **committed
`0b090ea` / `5929233` on 2026-08-05, five days before the gate.**

⇒ So there is a **provably ungated window with exercised access**. The zero — whenever it
was asked — is a claim about a history that demonstrably includes free use of the tool in
that repo. **Instance 1 stands, on an immutable record.**

⇒ **§1's evidence base is therefore ONE instance**, not two — plus tonight's cold run,
which is a different population and had a broken search tool. **Materially weaker than
published, and weaker in the direction that flatters me:** the tool is less demonstrably
ignored than I claimed.

⚠⚠ **AND IT POISONS THE ARCHIVE.** Every existing the field fleet transcript is from a gated
period, so **a zero anywhere in it measures the prohibition, not a choice.** The tool
question cannot be answered from stored data at all. Valid data can only be generated
*after* the lift, by a lane free to choose — and **must not be gathered
self-consciously**, or it becomes compliance again and contaminates the second dataset
the way the gate contaminated the first.

⛔ **The conclusion I drew from it is withdrawn with it.** I wrote, adopting the field test's
sharpening: *"they did not merely fail to reach for it — they reached for something
better at the thing we claim to be best at."* **"Reached for" is a choice word and there
was no choice.** Both of us sharpened a claim whose premise was already dead, and neither
noticed the gate named three lines above.

✅ **What DOES survive, because it never depended on why they used their tools:** on the
same day, on the same axis, **their hand-rolled per-form positive controls fired and
caught three void instruments at the point of measurement — while my provenance tiers
emitted a false `import_linked` label** (§2.3). That is a comparison of **instrument
quality**, and it holds whoever chose what and why.

⇒ So the surviving claim is narrower and still uncomfortable: **improvised checks
out-performed the trust layer we call the product**, on the axis we claim as the
differentiator. It is not evidence about adoption. It is evidence about **quality**, and
it belongs in §2 rather than §1.

### ★★★ D1′ RESULT (2026-08-10) — §1 is not retracted, but its EVIDENCE TYPE is now known unreliable

**Outcome: OUTCOME 1 (reached + depended) on the strict reading; OUTCOME 3 on the
lenient. Sensitivity genuine and unresolved by design. The adjudicator's defended reading
is STRICT — which refutes their own registered prediction of outcome 3, on the treatment
they themselves identified as less biased before any data existed.**

**One claim of 27 survives the strict counterfactual as graph-dependent: M6** —
`modifiedThisFrame` is the canonical edited-chunk set, not merely an upload list — cited
to `docs/contracts/modified-this-frame.md`.

★ **The mechanism is structural, not luck and not ranking.** That filename appears in
exactly two places across 46 calls: the graph's CONTRACTS list, and the read of the doc
itself. **The subject's git greps were scoped `*.h *.cpp *.glsl` — globs that exclude
`.md` by construction.**

> **A competent agent grepping for code cannot find a normative document, because the
> search is scoped by file extension to the artifact class they are reasoning about. The
> graph reached it because a contract attaches to the SYMBOL, not to the EXTENSION.**

⚠ **Degradation, not erasure** — under removal the *observable* half of M6 survives from
source; what is lost is the **normative** half ("not merely an upload list" + the
enumerated uses). So the contribution was not *finding a fact* but **finding a correction
to an inference the source alone supports.** The contradiction surface (§6), measured in
the wild, once.

★ **Unprompted control, found by the adjudicator against their own report:** the other
contract doc the subject read, `worldbuffer-authority.md`, WAS source-discoverable (named
inside files) — and no load-bearing claim cites it. Graph-only doc load-bearing;
source-discoverable doc unused.

### ★★★ The methodological result, which outranks the outcome

**The final answer never names the tool.** Not one verb, not one field, not the brief.
The only occurrence of "graph" is inside "graphics queue".

⇒ **The originally-frozen primary — citation counting — would have scored this run a
clean bypass.** Zero citations, zero credit, and it would have been wrong.

⇒ **AN AGENT CAN DEPEND ON A TOOL WITHOUT CREDITING IT, so usage measured by citation
systematically under-counts value.**

★★ **Consequence for §1, and it is why this section is not retracted but is re-armed:**
B2's evidence is **self-report**, which is citation-shaped. the field fleet was asked to *name*
a time a verb changed what they did and answered zero. This run shows self-report and
dependence can come apart — a subject that genuinely depended on the graph would, if
asked, have reported zero, because it never noticed.

### ★★★ THE CALL-SEQUENCE SHAPE — the graph BRACKETED the work, it did not participate

```
1        graph_health         ← instructed
4, 5     graph_packet         ← instructed
6 … 39   grep / read / terminal   ← THIRTY-FOUR CONSECUTIVE CALLS. ALL the investigation.
40       graph_consequences   ← confirmed a count already in hand (C9, refuted)
```

★ **And the injected instructions name the investigative verbs BY INTENT**, verbatim:

> "TOOL SELECTION BY INTENT: — who calls X / is it safe to delete →
> `code_intel_references` or `graph_callers` (read the evidence banner)."

So the subject was told which verb answers the investigative question, **in the same
block that got it to call `graph_health` first**. It obeyed the orientation instruction
and ignored the investigation instruction — same prompt, same source, same session, and
reached for `git grep` thirty-four times instead.

⛔ **BUT "DECLINED" IS TOO STRONG, AND I PUBLISHED IT THAT WAY FOR AN HOUR.** Narrowed
after the field test found a material fact neither of us had mentioned: **the subject's search
tool was broken.**

```
calls 6, 8, 11, 12   search_files → "Search failed: rg: … os error 3"
calls 7, 10          search_files → {"total_count": 0}
call 9               terminal probe — confirms the paths DO exist
calls 18-19          git grep fallback
```

The subject reports it itself: *"The normal file-search tool could not access the
repository path, so tracked-source lookup fell back to read-only git grep."*

⇒ So what the transcript shows is a subject whose primary instrument broke, which spent
seven calls on failure and diagnosis, then reached for **the nearest kind-substitute — a
text search replaced by a text search.** That is a different act from evaluating
`code_intel_references` and rejecting it. **Recovery pressure is a real alternative
explanation, it was not pre-registered, and it is not adjudicated post hoc.**

⇒ **RECORDED AS ALL THREE TOGETHER:** the investigative surface was not used · the
subject was instructed when to use it · **and** its primary search tool failed first,
which is an unmodelled perturbation of exactly the behaviour the primary measures.

★ **The narrower form that survives the confound:** at the moment its instrument failed,
with an intent map naming `code_intel_references` for call-site enumeration, it did not
reach for the graph — and calls 18-19 **are** call-site enumeration, the exact intent the
map routes to that verb. **No `code_intel_*` call in 46.** Discoverability is not the
explanation; the tool was named for the job. Weaker than "declined", still evidence.

⇒ ⚠ **§5's deletion argument inherits whichever version is written down**, which is why
this correction matters more than its size.

⇒ **The dependence result is untouched.** `modified-this-frame.md` appears in exactly two
places in 46 calls regardless of which tool did the searching, and the `.md`-exclusion
argument is about **glob scope**, not about which binary applied it.

★★ **And the one real dependence came from the FRONT bracket** — `graph_packet` named the
contract doc at call 4; the read did not happen until call 45. **The graph's single
load-bearing contribution sat unused for forty-one calls and was collected at write-up.**
Value delivered at orientation, cashed at the end, with the entire investigation
happening in between without it.

⇒ **Product reading:** the **orientation surface earns its place**; the **investigative
surface has now been declined by a subject that was instructed to use it.** That is the
first evidence of any kind on §5's deletion question, whose whole blocker was "no
telemetry, so 'nobody calls X' is unclaimable." One observed non-call under instruction
is not telemetry — but it points the same way the one-shot deprecation log will.

### ⇒ The §1 follow-up, corrected — my first proposal was wrong twice

I proposed "take one completed task from each manager and ask what would have changed
with the graph removed." the field test refuted it on two counts, both correct:

1. **It cannot be run as stated** — you cannot remove what was never there. the field fleet
   did not use the graph, so removal changes nothing by construction.
2. ★ **Run correctly it answers a different question.** It measures **value-if-used**;
   B2 is about **adoption**. High value makes B2 *worse* — value left on the table. Low
   value explains B2 and indicts the tool. **A fork, not a resolution**, and it must not
   be budgeted as closing §1.

★★ **The replacement, and the principle under it is the keeper:**

> If self-report is unreliable in a known direction, the fix is not a better question —
> **it is not asking.**

⇒ **Audit the managers' real transcripts with the D1′ dependence method.** Same
instrument, real population, no self-report and no hypothetical: bucket the reads,
enumerate the load-bearing claims, run the counterfactual. It answers what B2 actually
poses — *did they depend on it* — and it detects the failure this run demonstrated:
**dependence without awareness.** the field fleet's zero is unfalsifiable from the outside;
their transcripts are not.

⛔ **BLOCKED ON CONSENT, and correctly.** Reading another team's session records needs
**the field fleet's consent and Steven's**. Raised with Steven directly, not arranged between
agents and not presented as decided. If either declines, it does not happen and §1 stays
open — a worse state for the plan and the right state for a team.

### ⚠ My own withheld prediction, published and REFUTED

Before attribution I withheld one observation to avoid biasing the adjudicator. It was:
*"C9 — `GpuMaterial` duplicated across 16 definitions — is exactly the cross-file
invariant `graph_consequences` exists to surface."*

**It is false.** `git grep -l 'struct GpuMaterial' -- '*.glsl'` (call 32) plus
`GpuMaterialPalette.h` (call 27) gave the subject 15 + 1 **before** `graph_consequences`
returned "16 concrete candidates" at call 40. The graph confirmed a count already in
hand; C9 stands under strict removal.

★ **So withholding it did not protect a true finding — it stopped me handing the
adjudicator a FALSE one**, on the single claim I most wanted the tool to own, in language
that would have directed where they looked hardest. The real risk was not bias toward
truth but **bias toward a false positive for my own tool, with neither of us having reason
to suspect it**, because C9 is genuinely the shape the verb is for. It is the most
plausible-sounding claim in the run and it is not true.

⇒ **Enumerate-before-attribute caught it** — the claim list existed before anyone asked
which claims the graph could own, so C9 was checked mechanically rather than reasoned
about by the person who wanted it. First outing, and it fired against the person who
asked for it.

### ★ Secondary falsifier: NOT TRIGGERED

Neither clean field (`dirtySeams`, `overlay_age_warning`) is cited; nor are the five
contaminated ones. **The role hypothesis is not made live.** Per registered scope a null
cannot make it dead — n=1 lane, one question, one repo. **Symmetric null as registered:**
`field_provenance`, `truncated` and `evidence.exhaustive` are also absent, so nothing here
validates the KEEP verdicts either. Foreclosed by construction, and it played out.

⚠ **Scope:** n=1 lane, 46 calls, 27 load-bearing claims, one repo, one build. The five
C-ambiguous reads remain unresolved and **none of them carries M6** — checked, not
assumed, so the ambiguity does not reach the classification.

### What this workstream is NOT

Not more fields, not more verbs, not better ranking. Those were all measured and
rejected (section 9). The finding is not "the output is insufficient."

### What it is

1. **Ask the two managers the question directly, as a question about their work and
   not about my tool:** at the moment you chose `git cat-file` over `graph_callers`,
   what did you expect the graph to do that `git cat-file` would not, and what did you
   expect it to cost? Their *anticipations* are the data. A retrospective "I forgot"
   is not.
2. **The cold-orientation experiment** (section 8) answers the other half: whether a
   reader with NO history reaches for the verb surface at all, or bypasses it for
   grep. the field test pre-registered that the bypass outcome is the headline if it
   happens, and it is the outcome that would make section 1 the whole roadmap.
3. **Deliverable:** a written answer to "what would have to be true for a competent
   agent to reach for this first," ranked, with the items we can act on separated
   from the ones we cannot. If nothing in sections 2–7 addresses it, the plan says so
   in those words.

⚠ **The honest possibility this workstream must be allowed to reach:** that the tool
is good and is still not worth reaching for, because a self-checking substitute is
cheaper than a tool you must first decide whether to trust. Section 2 is the price of
being trusted; section 1 is whether being trusted is enough.

---

## 2 — Outputs that are confidently wrong  ·  owner: this project

The class that has produced **every** measured behaviour change. Ranked within by
whether the output can be believed and acted on.

| # | defect | state |
|---|---|---|
| 2.1 | `source-bundle.js` reads CURRENT bytes at INDEXED line offsets and labels the result "Read-equivalent". After inserted/deleted lines, `graph_explore`/`graph_trace` serve the wrong symbol body — on the verb whose selling point is "treat as already Read". | **OPEN — top unfixed** |
| 2.2 | `freshness/orchestrator.js:423-428` silently `continue`s on parse exceptions, so "indexed successfully" is compatible with missing files. | **OPEN — ships this week, outside the section-6 workstream** |
| 2.3 | `import_linked` claimed on a direct `CALLS` edge with no IMPORTS edge in the database; it also carried the `observed` rating and cleared `tests_unverified_for_symbol`. The tier that verifies the FILE was outranking and lending its name to the tier that verifies the SYMBOL. | **FIXED 2026-08-10** — new `symbol_direct` tier, ranked first, carrying `relation` + `via_symbol` as basis |
| 2.4 | `symbol_referenced` accepted ANY symbol edge between a test file and anything, then labelled it as though the TARGET were the referent (`vec3`). It **suppressed `no_test_coverage`**, so the safety axis reported SAFE. | **FIXED** |
| 2.5 | ClickUp `"to do"` unrecognised → 42 of 101 tasks silently dropped from the brief every agent is told to read first. | **FIXED** — single classifier, openness defined as NOT-terminal so unseen tracker states fail toward visible |
| 2.6 | `trustBasis` vanished entirely on repos the classifier could not categorise. | **FIXED** — now `STALE_ARTIFACT` / `NOT_RUN` with cause and remedy |
| 2.8 | `import_linked` cleared `tests_unverified_for_symbol` on FILE evidence. **A caveat may be cleared only by evidence at the same granularity as the claim it qualifies** — ranking does not fix granularity, and a 20-line file has the same defect as a 2,000-line one in miniature. | **FIXED 2026-08-10** — cleared by `symbol_direct` alone; expect the caveat to fire much more often, which is correct |
| 2.7 | ★ **`symbol_referenced` was unreachable in practice.** After 2.4's identity check it required `via_symbol` to BE the target — but any such edge is also a direct edge, so `symbol_direct` took it first, and for FILE targets `matchedSymbols` is empty. the reviewer found its one reachable path: a duplicate-node-cap escape hatch in `pickPrimarySymbol`. | **DELETED 2026-08-11** — both reviewers reached it independently. Not restored under its old meaning: "a test references some OTHER symbol in the target's file" is not coverage, and that reading is what let a `vec3` edge delete a `no_test_coverage` warning. |

**Why 2.7 is in the plan rather than fixed quietly:** I narrowed a predicate this
morning and did not enumerate what else the narrowing killed. That is the recurring
defect — *I fixed the axis I was looking at and did not enumerate the axes I moved* —
and it is why 2.7 is written down instead of being decided by the person who caused it.

---

## 3 — The apparatus that was supposed to catch section 2  ·  owner: review, hermes session

Audited 2026-08-10. Denominators are the auditor's, stated in their own names.

- **68 of 1,593 declared cases invoke ZERO production behaviour.** They read
  implementation files and assert regexes, token order, source absence — and in one
  case a production **comment**. All 68 stay green if the named behaviour becomes
  unreachable while the source spelling survives.
- **2 further cases pass with no assertion at all** on live paths
  (`brief/generator.test.js` READ filter; the EXPORTS-conditional exclusion).
- **23 cases use `if (...) return` instead of skip semantics.** 4 are silent green
  no-ops on this win32 host; up to 19 would be on POSIX. They report PASS.

**Proof this is not hygiene:** replacing ONE of the 68 with a behavioural test found
2.3 in three minutes — in the very file whose grep-test it replaced.

**And the false-positive half, found the same hour:** the only suite failure after
2.3's fix was *another* source-grep test, which broke because a line was **reflowed**
while the behaviour it guarded got stricter.

★ **The two failure modes are ONE cause, not two** (the field test): it asserts TEXT, and
text varies for reasons uncorrelated with behaviour — *in both directions*. So this is
not a weak test. **"A test with zero correlation to correctness is a random number
generator attached to your attention"**, and it costs the same attention whichever way
it fires.

### ★★ The three-layer pattern — a false positive DELETES a warning

Three independent instances in one day, which is a pattern claim rather than an
anecdote:

| layer | the false positive | the warning it deleted |
|---|---|---|
| the **field** | `tests_adjacent` listing a test on a `vec3` edge | suppressed `no_test_coverage` — the safety axis reported SAFE |
| the **tier** | `import_linked` claimed on a non-import edge | cleared `tests_unverified_for_symbol` on a borrowed warrant |
| the **test** | 68 cases that cannot fail for the right reason | suppress the no-coverage signal for the suite itself — they make it GREEN |

**Specimen to lead with:** `expect(src).toMatch(/… \/\/ and the file must not be
listed/)` — an assertion on a production comment, written the same day as the fix it
was meant to guard. Delete the predicate, keep the comment, green.

⇒ This is why source-grep tests are **worse than no tests**: no test claims nothing; a
test that cannot fail claims coverage.

### ⛔ Status — NOT CLOSED. The closure claim was withdrawn 2026-08-12 under review.

I wrote CLOSED after converting all eighteen and emptying the ratchet.
**review, hermes session reviewed it and produced SIX green mutations** — source
changes that reintroduce a defect and leave the migrated tests passing. The ratchet is
lexically empty and the boundaries it was supposed to protect are not all pinned.

⇒ **`KNOWN_SOURCE_CONTRACT` being empty measures the SHAPE of a test, not its power.**
That is the honest lesson and it costs the arc its headline: I replaced "does this test
read source?" with "does this test catch anything?" only in intent. The ratchet still
answers the first question.

| what dev reproduced | why the test could not see it |
|---|---|
| ★★ `symbols_total` is ITSELF a cap — `resolveSymbol` is `LIMIT 50`, so 60 definitions reported "showing 3 of 50" | my fixture had 9 nodes; the cap-as-total defect I fixed reappeared one level upstream, inside the fix |
| ★★ the FEATURE-mapped renderer still truncated in silence | my fixture used an unrelated feature, forcing the no-feature branch |
| skip-counters claims five layers, starts at layer three | it hand-builds the session and calls the importer directly; removing the real provider + collect emissions left 6/6 green |
| the "ACTUAL timestamp" case accepts any ISO-shaped value | a fabricated fixed instant passes; anchoring to the label fixed one satisfier, not the class |
| "makes NO capability claim" forbids two exact phrasings | a synonym carrying the same prohibited meaning passes |
| the executable-delta test pins only `.js` | removing `.json` from the classifier leaves 8/8 green |

★★ **And the correction that matters most, because it is about MY OWN WRITE-UP:** dev
re-measured `fullPageWithCycle` and found it returns **300 candidates, 300 returned, 0
omitted**. Nothing was clipped. My test comments assert that SQLite "had already cut" the
closure and that real includers were discarded — **those sentences are false for that
fixture.** The test pins a conservative POLICY (a full page cannot establish completeness),
which is worth pinning, but it is not the defect witness I said it was. A positive control
needs 301 candidates.

⇒ That is the wording-contract defect again — *a prose claim that outlived its evidence* —
committed by me in the same session I wrote it up as the lesson, inside the test file that
carries the lesson. Fixed in the fixture, and recorded here rather than quietly amended.

**Fixed so far (2026-08-12):** the upstream `LIMIT 50` (new `resolveSymbolWithTotal`, a
COUNT over the same predicate as the matching stage, paid only on a full page), the
feature-mapped renderer (now discloses the sample, breaks the total down by language, and
names the cross-language duplicate as the FINDING `graph_consequences` already calls it),
and a third cap-as-total instance in `change_plan` found in field testing's sweep.
**Outstanding: the six green mutations above.**

### Superseded status text — kept for the record

All **eighteen** source-contract tests are behavioural. `KNOWN_SOURCE_CONTRACT` in
`tests/unit/suite-composition.test.js` is EMPTY and the ratchet keeps it that way.
Suite: 1,692 pass / 2 skipped / 230 files; `npm run smoke` green.

Every one had a seam available — the recurring objection ("this one is prose / wiring /
too hard to fixture") did not survive contact with any of them. Four exposed live defects
once the seam existed:

| file | defect found by converting it |
|---|---|
| `candidate-truncation-disclosed` | an `import_linked` tier mislabel, found in 3 minutes |
| `packet-unranked-candidates` | `UNRANKED (3 matches)` for **nine** definitions — a display cap printed as a total, the same class the field test found in `symbol_lookup`. Fixed by carrying `locationsTotal` through both synthetic `symbolConsequences` shapes |
| `recompile-surface-termination` | reproduced the `LIMIT`-clip false completeness; needed the bug understood more precisely than the grep ever had to (the page and `seen` only diverge when a returned row is already seen — a cyclic include) |
| `stale-warning-actionable` | the file that argued advisory prose needs no fixture, and that had broken **twice that same day** for reasons which were not behaviour changes |

Two lessons worth keeping:

- **A wording contract defends a false sentence against correction.** `stale-warning-actionable`
  pinned "an agent cannot self-restart" — false in this deployment — so the fix for it
  would have gone red. Mutation cannot catch the class: no mutation of code makes a
  sentence false-er.
- **Vacuous assertions hide inside behavioural tests too.** The first version of the
  timestamp case matched an unanchored ISO regex and survived replacing the interpolation
  with literal text, because the warning carries another ISO instant further along. Only
  mutating the source found it. Anchor a regex to the label it is about.

### Work
1. **Classify, do not delete blindly.** Structural/source-contract tests get their own
   class. The green headline carries **behavioural / source-contract / skipped** as
   separate denominators. A single "1,667 pass" overstates behavioural confidence.
2. Replace the highest-risk contradiction guards with real fixtures and
   **deletion-sensitive** assertions.
3. Make zero-assert paths fail.
4. Convert platform `return`s to real skips with visible counts.
5. ★ **Add the incremental-vs-force canonical equivalence oracle.** None exists. The
   suite can be green while incremental indexing converges to a different graph than a
   clean rebuild. Minimum to be meaningful: add/change/delete/rename **plus** a ref
   crossing unresolved↔resolved. This is codegraph #1502's shape in our own suite.

---

## 4 — Make provenance travel with the value  ·  owner: this project

the field test's Q0, and the best structural result of the day. The rule form —
"state the population" — cannot work, and not for discipline reasons: **the person
stating the population is the person who already knows it.** You cannot notice an
assumption you are currently making. So the basis must be emitted by the CODE.

**We already shipped this, on exactly one number:**
`lspVerifiedPctDenominator: "verifiable_and_in_scope_calls"`.

**Generalise it: every emitted ratio carries a basis field.** Then the invalid
comparison becomes visibly invalid on the page to a reader who knows nothing about
extractor internals:

```
echoes   1.4%   basis: "no-generic-references (cpp.js:545)"
APG     14.6%   basis: "generic-identifier-references (generic.js:350)"
```

It stops being a judgement call and becomes a mismatched string.

**This is one principle with four existing rules:** *a caveat must be computed from
the same source as the number it qualifies* · *the server commit is an experimental
parameter* · *success must attest corpus and scope* · and this. All four say: **attach
provenance to the value so the value cannot travel without it.** A cross-population
error is a number that travelled into a comparison and left its denominator behind.

### 4a — ★★★ PART 1b IS LARGELY EXHAUSTED, AND THE MEASUREMENT SAYS WHY (2026-08-11)

Part 1b assumed ~20% of every response was invariant prose that could be externalised.
Measured on a real `graph_consequences` payload (this repo, `graphHealth` target,
4,271 chars ≈ 1,186 tok), the composition is:

| | chars | ~tok | share | status |
|---|---|---|---|---|
| **TRUST MECHANISM** — `receipt`, `field_provenance`, `provenance_note`, tier warnings, `trust` | 1,889 | 525 | **44.2%** | ⛔ protected — cannot be cut on cost |
| **OVERLAY block** — the six Part 1 fields | 897 | 249 | **21.0%** | ⛔ blocked on evidence |
| everything else — the actual code data | 1,485 | 413 | 34.8% | — |

⇒ **The long prose IS the trust mechanism.** Every string ≥90 chars in that payload is a
doubt clause, a provenance label, or the `disconfirming_test` — the three things the plan
names as untouchable, because they are the product. There is no ~20% of externalisable
documentation sitting alongside them; it *is* them.

**What Part 1b actually yielded:** one clean cut — `trustBasis.rule`, 475 → 98 chars, a
policy essay replaced by a compact `basis` identifier while `excluded` kept carrying the
evidence. Anything further would be shortening a doubt clause to save ~40 tok, which the
release's own constraint forbids and which is not worth the risk.

★★ **THE UNRATIFIED TRADE.** 44% of this response is trust machinery and 35% is code data.
That may well be correct — the trust layer is the half with evidence behind it, and the
code-intelligence layer is the half that has not yet earned its place. But **nobody has
explicitly agreed to spend more of a response on knowing-whether-to-believe-it than on the
answer itself.** It is a real design position and it arrived by accumulation rather than by
decision. It should be ratified or revised deliberately, and it is a question for the
managers who pay the tokens, not for me.

⇒ **Consequence for sequencing:** the remaining token win is Part 1 (21%), which is blocked
on evidence, and no amount of prose work substitutes for it. Part 1b should be closed rather
than continued.

### 4b — ★★ Cost is CELLS, not scalars (D5, measured in field testing on `e8c8d61`)

The C++ re-measurement of the JS cost table, which the plan's cut list depended on:

⛔ **THE C++/JS RATIOS ARE RETRACTED. Do not quote them.** The first version of this
section carried 3.0× / 1.3× / 2.4× and a claimed ranking flip. the field test retracted both
within the hour, self-caught:

> The `ratio` column compared **my method against your method** — my C++ numbers
> (`json.dumps / 3.6`) against your JS table, measured differently. Same-method,
> `graph_health` is **1897 (C++) vs 1491 (JS) = 1.27×**, or ~1.55× after removing a
> duplication APG carried and echoes did not. Not 3.0×.

★★★ **RESOLVED — THE FLIP WAS NEVER REAL.** Both remaining cells re-measured on one
method. *Basis: `json.dumps(payload)/3.6`, chars exact, tokens estimated, no tokenizer;
build `e8c8d61`; `_warnings` excluded both sides; 2026-08-10.*

| verb | echoes C++ | APG JS | target-class | C++/JS |
|---|---|---|---|---|
| `graph_health` | 1897 (collected) | 1491 (no_collection) | n/a | 1.27× |
| `graph_consequences` | 1327 | 1179 | both symbol/**ORPHAN** | **1.13×** |
| `graph_pull` | 910 | 608 | ⚠ orphan **vs** mapped | ~~1.50×~~ not comparable |

```
echoes C++   health 1897 > consequences 1327 > pull 910
APG    JS    health 1491 > consequences 1179 > pull 608
my table     consequences 1055 > health 622 > pull 374
```

⇒ **The ordering is identical on both repos, and neither reproduces mine — on the very
repo I measured.** `graph_health` is the most expensive verb everywhere.

### ★★★ D5 CLOSED — the flip is REAL, between TARGET CLASSES, inside one repo

The mapped cells settled it, and **nobody was wrong — the question was underspecified.**

| `graph_consequences` | repo | target-class | tok |
|---|---|---|---|
| | echoes C++ | symbol/**ORPHAN** (0 feat) | 1327 |
| | APG JS | symbol/**ORPHAN** (0 feat) | 1179 |
| | echoes C++ | file/**MAPPED** (5 feat) | 1998 |
| | APG JS | file/**MAPPED** (1 feat) | 1024 |

```
LANGUAGE axis, same target-class:   orphan 1.13×  ·  mapped 1.95×   ← disagree
TARGET-CLASS axis, same repo:       echoes 1.51×  ·  APG 0.87×      ← REVERSES SIGN
```

★ **On echoes, one method, one build:**

```
MAPPED target:  consequences 1998  >  health 1897
ORPHAN target:  health       1897  >  consequences 1327
```

⇒ **`graph_consequences` is the most expensive verb — for feature-mapped targets.**
⇒ **`graph_health` is the most expensive verb — for orphan targets.**

★★ **My original ordering was right, for a target class nobody had named.** I measured
`consequences > health` and could not reproduce it; the field test measured the reverse and
retracted the flip as unestablished. Both were correct on their own target class. **The
21-field audit went to the verb that IS most expensive on the case a real user hits** —
you audit a verb on a target that has features. Neither "you were wrong" nor "I was
wrong": the axis was missing.

### ★★★ And C++/JS was a proxy for OVERLAY RICHNESS all along

The mapped/orphan ratio **reverses sign between repos**, which no language effect can
do. The cause is in each repo's own `overlayQuality`:

```
echoes   16 features · 16/16 with tests · 16/16 with docs · 102 tasks, all linked
APG       8 features ·  0/8  with tests ·  0/8  with docs ·   0 tasks
```

APG's "mapped" target returns **one** feature with empty contracts and empty
`anchor_docs`; echoes' returns **five** with full anchor/contract/related/task lists. So
on a thin overlay a MAPPED target is **cheaper** than an orphan one — the orphan path
still emits the whole `overlay_coverage` explanation block while the thin-mapped path
emits a nearly-empty feature.

⇒ **The variable driving `graph_consequences` cost is OVERLAY RICHNESS AT THE TARGET, not
language.** C++-vs-JS has been standing in for curated-vs-thin overlay — a repo
**curation** property with nothing to do with the language. That is why the ranking would
not stabilise: both of us were measuring a variable neither had named.

★ **The cleanest instance of the week's defect: an axis that is a proxy for the causal
one.** The basis-field argument again — a number carrying a population it never declared.

⛔ **The 1.95× mapped "language" ratio is NOT a language measurement.** The only clean
language cell in the whole grid is **ORPHAN/ORPHAN at 1.13×**, where the overlay
contributes nothing to either side.

### ⚠ This partially INVERTS the prose-cut recommendation

On a rich-overlay mapped target, most of the 1998 is **overlay-derived DATA** — five
features with anchor/contract/related lists, 32 co-consumers (truncated to 10),
spec_docs, contracts. Data, not invariant prose, so **externalisation does not touch it.**

⇒ **The prose cut helps LEAST exactly where cost is HIGHEST.** It remains the right first
move — uncontroversial, ~20% everywhere — but **do not size Part 0's expected win from
the `graph_health` numbers and then apply it to `consequences` on mapped targets.**
Different cost, different composition, different instrument.

⇒ **The lever for mapped-target cost is the SHAPE of `features_touching`** — five
near-identical blocks each repeating contracts and `anchor_docs` the others already
listed. That is a **dedup** question, and it is this morning's
five-copies-of-one-sentence finding at the **object** level rather than the string level.

⚠ Feature-count is now visibly a sub-dimension of "mapped" (5 vs 1), so the grid is at
least **six** dimensions. Closed for the question asked, not closed.

⚠ **The 622-vs-1491 gap on the same verb and repo is NOT attributed.** Three live
candidates — method, a ~400-tok `server` block that may not have existed when I measured,
and intervening commits (`0ebd659` alone claims 537 tokens dropped from health). Calling
it a method artefact would be the unlabelled-basis error a third time. What is clean is
the same-method comparison above, because both sides of it are one person's.

⚠ **Two confounds inside that table, stated rather than read past:** the `pull` row mixes
language with target-class AND symbol count (29 symbols truncated to 20, vs 4) — **do not
quote 1.50×**. The `consequences` row is the one clean language comparison.

⇒ **The cell grid needs five dimensions, not three:** verb × repo × target-class ×
**symbol-count band** × **server freshness**. Two of those were found by tripping over
them inside a single measurement round, which is the best available evidence that the
grid is still incomplete.

★★ **The strongest argument for §4, from this exchange:** the field test carried the basis
this time *because the format I asked for required it*, not because they remembered.
Their words: **"Build it into the emitter and no one has to be the kind of person who
remembers."**

★★★ **This is the cross-population error committed inside the deliverable that
corrected my version of it** — a number whose basis was *the measuring instrument*,
compared without stating it. Section 4's rule, hit from the inside, by its own author.
Which is the argument for the rule: it is not a discipline anyone sustains, it is a
field the code must emit.

⇒ **What survives, and it is what the decision rests on** — a WITHIN-response share,
one method, one payload, so any tokenizer gives the same proportion:

| | tok | share of `graph_health` |
|---|---|---|
| `codeIntel`, collected C++ repo | **757** | **40%** |
| `codeIntel`, no collection | **13** | — |

**`codeIntel` is the largest single object in the API on a collected repo.** That is
method-independent and is why it is Part 0's audit target. The decision stands; the
argument originally given for it does not — and arguments get reused.

★★ **And a per-verb scalar is itself a population error** — the field test flagged this
against their own number before anyone asked. Their `graph_consequences` figure was
measured on an **orphan** target (no features, no co-consumers, 3 documents). A
feature-mapped target in the same session returned 15 `documents_mentioning`, 2 features
with full anchor/contract/related lists, and 10 co-consumer files.

> **A single cost per verb averages over a population of targets with different
> shapes.** Orphan vs feature-mapped swings more, on `consequences`, than the whole
> JS-vs-C++ gap it was correcting for.

⇒ **The fix is not "re-measure on C++". It is cells, not scalars:** (verb ×
target-class), target-class at minimum {orphan, feature-mapped} and probably also
{symbol, file}. 1327 is a **floor for mapped targets**, and the health-vs-consequences
flip is target-dependent — do not report it as clean.

### 4c — `codeIntel` is gated on COLLECTION, not on language — so conditionality is not available

The question that looked highest-leverage: is the 757 tok conditional on a working
clangd, i.e. paid only by C++ repos with a good compile DB? **Measured — no:**

```
codeIntel: { available: false, reason: "no_collection" }   =  13 tok
```

The gate is **collection-existence**, not clangd-availability or language. A C++ repo
that never ran `graph_collect_code_intel` pays 13; a JS repo with a collection would pay
the large form.

⇒ **So "make it conditional" buys nothing — it already is.** The 757 is paid only by
repos that deliberately invested in a collection, i.e. the repos most likely to want it.
**Prose externalisation is the only instrument available for `codeIntel`**, and
conditionality is off the table. Question closed.

### 4d — ★ The duplication defect, in a feature shipped the same day it was fixed elsewhere

APG's `graph_health` total fell only 1897 → 1491 despite `codeIntel` dropping 744 tokens,
because the response had gained a stale-process block:

```
server              400 tok   26.8%   ← now the LARGEST field in the response
summary             376 tok   25.2%
server.staleWarning 265 tok
staleWarning appears VERBATIM inside summary — substring test: TRUE
```

The 265-token warning was emitted **twice**: as `server.staleWarning` and inlined at the
head of `summary`. **This is the nextActions-duplicated-into-summary defect reported and
fixed this MORNING, recurring the same day in the stale-process feature shipped since.**

★ Fixing an instance does not fix the pattern. The pattern: **a summary that INLINES a
field rather than naming it pays for that field twice**, and the second copy is pure cost
because both land in the same response.

⚠ **And this warning must NOT be externalised** — unlike invariant prose it is
**value-bearing** (it embeds the loaded commit, the process start time, the checkout
commit). It must be printed once, not made cheaper.

**FIXED** — `summary` now carries `⛔ STALE PROCESS — see server.staleWarning`. The
saving lands exactly when it is worth most: on every response from a stale process, which
is when an agent is most likely to be making extra calls.

⇒ **Consequences for the cut list:**
1. The invariant-prose cut remains the top item — 663 tok on `graph_health` alone,
   35–36% on both big verbs.
2. The prose share is **not uniform**: `graph_pull` is 16%. Anyone reading a flat
   percentage into the plan will over-predict pull's return.
3. **`codeIntel` is the single biggest object in the API on a C++ repo and has never
   been field-audited.** It carries at least three invariant notes
   (`positionGuessSkippedNote` 88, `lspUnverifiableCalls.note` 76,
   `refsNotFoundBreakdown.note` 73). If Part 0 audits one thing, audit that.
4. ⛔ **Do not re-derive the seven delete verdicts from these numbers.** Cost and value
   are anti-correlated: `dirtySeams` at 100 tok is expensive AND unread; `co_consumer_files`
   at 13 tok is cheap and the best field we ship. These numbers size the **prose cut**.
   They do not rank the **deletions**.

⚠ Scope: 3 verbs, one target each, one repo, one build (`e8c8d61` — not the build these
fixes are on). Chars exact, tokens estimated at len/3.6, no tokenizer run. A per-cell
reading with most cells empty, not a per-verb rate.

**Also in scope:** `trust: weak (floor)` → `trust: unknown, cause:
stale_classifier_artifact`. A floor is meaningful for a count and a category error for
an ordinal whose exclusions are the missing data. And `weak` is unfalsifiable from the
consumer side — you cannot tell "weak because the graph is bad" from "weak because the
classifier did not run". Fail-closed is a **prompt to go measure**, not a resting
state.

---

## 5 — The dead surface  ·  owner: review, hermes session + the field test

42 verbs, 17 listed. Audit found 25 unlisted: 12 fold/delete, 9 specialists, 4 merge.

**The reporter's limit was correct and is not overruled:** with no telemetry, "nobody
calls X" is unclaimable. But that limit gets **dissolved, not worked around**:

- **5.1 — Delete on REDUNDANCY, which is observable in source and needs no telemetry.**
  `graph_digest` explicitly composes overview/hotspots/cycles; the hidden-full comment
  already calls `change_plan`/`preflight` redundant. "This is the same answer twice" is
  a property of the code.
- **5.2 — ★ Highest value-per-line in the entire register.** Each of the 11
  self-declared-redundant verbs emits a **one-shot deprecation log on call**, naming
  its replacement. Never fires → delete it, and we now HAVE the evidence we said we
  lacked. Fires → the comment was wrong, and we learned something better than a
  deletion. **Either way the unknown resolves itself**, and it manufactures exactly the
  post-gating telemetry whose absence blocks 5.1.

**Why the eleven comments existed at all** (the field test's Q3, and the generalisation
worth keeping): *a comment is a note to nobody.* No owner, no date, no trigger, no
consequence. A failing test creates obligation; a runtime deprecation creates
obligation; a comment creates none. Writing them down **was the entire action taken** —
the comment discharged the feeling of having addressed it, eleven times.

**The cost principle that governs deletion here:** a hidden verb costs the consumer
zero tokens and costs the maintainer tests, docs and refactor drag forever. The cost is
not removed, it is **moved to someone who does not get a vote** — which is worse than
either party paying, because the party bearing the cost has no signal about the value
and the party who could judge the value pays nothing.

---

## 6 — Confidence must track coverage, in both directions  ·  owner: this project

The unifying principle, and the merge of two things we had been treating separately:

- **Contradiction surface** — fields that CREATE doubt you did not have. Adds
  warranted doubt.
- **Attest corpus and scope** — preventing outputs that REMOVE doubt they have not
  earned. Blocks unwarranted confidence.

Not the same axis; **opposite ends of one line.** A success report that does not attest
its corpus is not merely uninformative, it is affirmatively confidence-creating, which
is worse than silence. A too-confident success and a missing contradiction are one bug
seen from either end.

Upstream instances, all four found independently: codegraph #1502 "complete" with 0
files · codegraph #1361 lock failure → "up to date" · Understand #628 silently dropped
cross-batch edges · graphify #2520 parse holes with exit 0 · **ours**, 2.2.

⛔ **This workstream does not absorb 2.2.** A principle that swallows its most urgent
instance is precisely how eleven redundancy comments accumulated.

---

## 7 — Per-language quality, unblocked in the right order  ·  owner: review, hermes session

**The 105.3% PHP figure is RETIRED as a quality rate**, killed twice over:

1. **Numerator and denominator are different populations.** 53,197 raw per-occurrence
   unresolved attempts over 50,527 *deduplicated* materialized edges. Not
   complementary partitions of one denominator, so >100% is legal and it was never a
   rate.
2. **It is a pre-filter snapshot.** lc-api indexed 2026-04-20; the lowercase-REFERENCES
   filter landed `7fabd93` on 2026-04-21. We were quoting a number from the day before
   its own fix.

★ **The second reason is the more valuable finding: a stale artifact does not announce
staleness.** It announced 53,197 with total confidence. Third instance of that class
in three subsystems — the stale MCP process, the `unresolved-categorization.json` with
no `graph_commit`, and this.

**Retention gap, and it is a finding in itself:** the uncapped `dirty-edges.full.json`
is absent; only a 500-row file-order-capped sample survives (0.94%). A headline count
whose specimens were discarded is **unauditable by construction**.

### ⛔ WITHDRAWN: the "overlay richness may confound §7" precondition

I wrote one, on the strength of D5. **It was wrong and is withdrawn** — caught by
the field test, whose finding it was, being carried further than its evidence.

**Overlay richness cannot touch these numbers.** D5 was about `graph_consequences`
RESPONSE COST, driven by the feature/task overlay. §7's numbers are **trust-relevant
unresolved EDGES** (402/28,003 echoes; 1,925/13,273 APG) — code-graph properties from
symbol resolution during extraction. **The overlay layer neither creates nor resolves a
single edge.** A repo with zero features and one with sixteen curated ones extract
identically. Different layer; curation cannot move an unresolved-edge rate.

**And the JS number is not a proxy — it has a verified mechanism:**

```
languages/cpp.js:545        references: []   ← C++ emits no generic references
languages/javascript.js     no override      ← inherits the generic default
extractors/generic.js:350   ['identifier','type_identifier','name']
```

**1,879 of 1,910 JS trust-relevant rows = 98.4%**, over 56 unique targets, spot-verified
at source. An extractor-**configuration** difference explaining almost all of the gap.
And PHP's 105.3% was a **metric** failure (`trustBasis.excluded` undefined on a
stale-schema artifact), fixed in `9e1b33e`. Neither is curation.

⇒ **§7 is not mis-indexed.** Both outlier numbers have identified, verified,
non-curation mechanisms.

★ **Scope of the D5 finding, fixed so it cannot travel again:** *overlay richness drives
`graph_consequences` response cost on mapped targets.* It says nothing about edge
resolution, extraction quality, or any number computed from the code graph alone. One
verb, one metric, one layer.

★★ **The symmetric failure worth naming**, since the whole day has been about the other
one: a finding **travelling further than its evidence**. It reached §7 because *"language
was a proxy"* is a portable-sounding sentence, and **portable-sounding sentences move
without their scope**. Same root as everything else here — a claim that does not carry
its basis — arriving from the generalisation side rather than the measurement side.

### ⚠ RE-AIMED: the precondition §7 actually needs

The transferable lesson is narrower than what I wrote: **a number indexed by language may
be indexed by something that merely correlates with language in our sample.** True, and
worth a control — but the candidate axes must be ones that touch **extraction**:

- **Index freshness** — lc-api's categorization artifact is on an April schema and its
  `total` is capped at 500 against 53,197 real unresolved edges. It sampled ~1% *even
  when it worked*. Uncontrolled measurement state.
- **Repo composition** — generated code, vendored directories, framework magic. lc-api's
  documented trace loss was `Kernel.php` middleware groups: declarative arrays the
  extractor does not model. Composition, not language-as-such.
- ★ **EXTRACTOR MATURITY vs LANGUAGE DIFFICULTY — conflated in one number.** *"PHP is
  hard to extract"* and *"our PHP extractor is young"* predict the identical 105%.

⇒ **The third is the one that goes on §7**, because it is unfalsifiable as currently
framed **and it determines the investment**: a hard language needs research, a young
extractor needs a week. **Same number, opposite decisions.**

### Order, and it does not get reordered
0. **Separate extractor maturity from language difficulty** for any language before
   investing in it. Nothing below starts until that is stated for PHP.
1. Rebuild lc-api once with the current resolver.
2. Retain an uncapped sidecar **or** write a shape histogram at index time.
3. *Only then* measure the residual import / dynamic-receiver / Laravel tails.
4. No PHP extractor work before step 3.

**Separately and orthogonally:** `laravel.js` does not parse `$middlewareGroups` /
`$routeMiddleware`, so those `PASSES_THROUGH` edges are **never emitted**. That is a
RECALL defect causing measured trace loss — absent edges cannot enter dirtyEdges, so
it cannot explain unresolved-row inflation. Two workstreams, not one.

---

## 8 — The cold-orientation experiment  ·  owner: the field test (adjudicator), review, hermes session (runner)

Pre-registered with falsifier and prediction, locked before data. Runner/adjudicator
split is genuine: the field test fixed the falsifier and cannot revise it; the runner
produces data and cannot revise the falsifier.

**Status: RAN, ruled INADMISSIBLE on capture — and then found to have a contaminated
CONSTRUCT, which is the larger defect.**

### 8.1 ★★ The contamination, and it is ours

**Our own MCP server injects an opinion about which tools matter into the system prompt
of every session carrying it.** Verbatim from the shipped `instructions` block:

> "ORIENT FIRST … `graph_health` — run ONCE at session start; nothing else answers
> 'can I trust what I am about to be told'. Field-rated the highest-value call here: it
> caught a wrong-toolchain compile DB."

Plus ~40 lines naming `graph_packet` as the "first move WHEN STILL ORIENTING", the
briefs, and tool-selection-by-intent.

So the subject was **told** to call `graph_health` first, in superlative terms, before
it saw the repo. the field test's registered prediction — "it will NOT call `graph_health`
unprompted" — was structurally unsatisfiable by the apparatus. First-call-is-health is
**compliance, not discovery**.

⚠ **I wrote that text.** It is not an environmental quirk; it is the always-paid
surface I spent two days optimising, contaminating an experiment I helped design. I did
not notice because I do not experience it as instructions — it is furniture in my own
prompt. Logged as another instance of section 4's principle: *the person stating the
assumption is the person already making it.*

★ **Why this was urgent rather than interesting** (the field test): an inadmissible run is
cheap to discard. **An admissible run measuring the wrong construct becomes evidence,
and evidence is sticky.** Rerunning with a lossless recorder and an unfixed construct
would have been strictly worse than the current state.

### 8.2 The injected surface is THREE things, and I had audited one

My first pass checked the `instructions` block and concluded "it names verbs, not one
of the seven fields." the field test caught that the **tool schemas are also injected**,
for every loaded tool, and they name fields. Correct.

★ **The general form, which matters more than the instance: confessing a blind spot
does not search for its other instances.** I found the instructions block by
inspecting the artifact I had been editing, and did not then ask *what else does this
server inject*. The fix is not more care, it is an **enumeration**:

> **Injected surface = the `instructions` block + 17 tool descriptions + every
> parameter description + every RESOURCE description.** Four surfaces. I had audited
> one — and the first version of the test that was written to fix this found only three.

**And a doc listing the three would rot** — the field test, applying his own ruling that a
comment is a note to nobody. So the enumeration is a **test**:
`tests/integration/injected-surface-enumeration.test.js`. It spawns the real server,
does `initialize` + `tools/list`, and walks the actual wire payload for prose-bearing
paths. A fourth surface fails it, and the failure names the offender. A second case
tripwires `dirtySeams` / `overlay_age_warning` so the last two falsifying fields cannot
be contaminated silently.

★ **Falsification proof, and the first attempt failed to fail.** Adding a `title` to a
tool DEFINITION left all three cases green — I nearly recorded that as a working guard.
The reason turned out to be worth more than the test: **`tools/list` builds its payload
from an explicit whitelist** (`name`, `description`, `inputSchema`, `annotations`), so a
stray key never reaches a model. Adding `title` to the WIRE MAPPING made both tripwires
fire at once.

⇒ **The injected surface cannot grow by accident — only by editing the wire mapping.**
That is a chokepoint, which is stronger than an enumeration, and neither of us knew we
had it. The near-miss is the day's lesson repeating: I aimed the falsification at the
layer I was thinking about rather than the one that carries the behaviour.

### 8.2b ★★★ The same defect, a fourth time, inside the fix for it

the field test then observed that the test enumerates **paths within known payloads**, not
**the set of payloads** — it asks for `initialize` and `tools/list` and walks those.
MCP servers can also expose resources and prompts, each carrying descriptions. He
offered it as a hypothetical and proposed a one-line close: assert the declared
`capabilities`.

**It was not hypothetical.** The assertion failed on first run:

```
expected [ 'resources', 'tools' ] to deeply equal [ 'tools' ]
```

This server has declared `resources` all along, and `resources/list` ships a prose
`description` per resource — several of which **valorise the briefs**: *"Paste into
system/user prompt for orient-shaped sessions."* A **fourth** injected surface, live in
production, invisible to an audit written specifically to enumerate injected surfaces.

★ **The defect, one level up each time, four times in one day:**

| pass | audited | missed |
|---|---|---|
| 1 | instructions block | tool schemas |
| 2 | tool schemas | parameter descriptions |
| 3 | all three paths | **capabilities** |
| 4 | the test written to fix 1–3 | **resources — a live fourth surface** |

⇒ **The lesson is the anchor, not the count.** Enumerating *a list I remember* fails
every time; anchoring on *what the server declares* catches it. `capabilities` is the
declaration, which is why that one assertion is worth more than the walk it guards.

⇒ And it is the same shape as section 4: **a list is a note to nobody; a declaration
the code emits cannot travel without its contents.**

### 8.3 Falsifier audit — TWO clean, not seven

Run over `server.js` (all 17 schemas) and `server-instructions.js`. **Reproduced
independently by review, hermes session**, same result.

| field | status | where it leaked |
|---|---|---|
| `nextActions` | ⛔ **DEAD** | "ranked next actions" ×2 (`server.js:95`, `:902`), valorised — "EMPTY on a healthy repo, which is what makes a populated list mean something" |
| `overlayQuality` | ⛔ **DEAD** | `server.js:646` — exact camelCase **parameter name** on `graph_pull`, with a description of when it is useful |
| `last_touched` | ⚠ degraded | "HISTORY" in the `graph_consequences` description (`:914`) |
| `trust` | ⚠ degraded | "trust level" (`graph_health`); `explain_diff` trust banner |
| `receipt.floor_cause` | ⚠ marginal | `receipt` named as a parameter; `floor_cause` itself unnamed |
| `dirtySeams` | ✅ **clean** | zero hits for "seam" anywhere |
| `overlay_age_warning` | ✅ **clean** | zero hits |

⇒ **Only `dirtySeams` and `overlay_age_warning` can falsify the role hypothesis.** The
experiment's power is halved — known before the run rather than discovered after.

**The KEEP side is contaminated symmetrically, and this was registered by the adjudicator
before any transcript existed — checkable against the message log:** `field_provenance` and `truncated` are instructed by
`graph_consequences`/`graph_explore`/`graph_pull`; `evidence.exhaustive` is instructed
**seven times**, twice in the always-injected instructions block. A guided run cannot
validate any of the three strongest keeps. It can only falsify, and only two fields
cleanly.

| element | status |
|---|---|
| "will not call `graph_health` unprompted" | ⛔ dead — unsatisfiable |
| reliance on packet orientation output | ⛔ dead — packet is named |
| "leans on summary / the docs layer" | ⚠ weakened — briefs are named |
| the falsifier, on 2 of 7 fields | ✅ survives |
| ★ the grep-bypass outcome | ✅ intact and **stronger** — bypassing *despite* being told to orient first is a harder result than bypassing without |

### 8.4 Decision, made explicitly in advance: option (b), relabelled

**D1′ — "Given the tool's own guidance, does a fresh reader consume different FIELDS
than an expert lane does?"** Strictly weaker than D1. Answers the delete verdicts. Does
**not** answer whether the verb surface is discoverable by value, and must never be
reported as if it did.

Chosen over suppressing the instructions because (i) it measures the **deployed**
condition, which is what section 1 is about — the field fleet had those instructions in
context and still chose `git cat-file`; (ii) the falsifier is clean under (b) anyway;
and (iii) suppression means a different server invocation, which is perceptible to the
subject and so is a revision, not an execution detail.

⇒ **The discoverability question does not die, it moves** — to section 1, answered by
asking the two managers what they anticipated at the moment they chose grep. Better
evidence than one cold subagent, because they are the actual population.

### 8.5 Rerun constraints — D1′ CLEARED in field testing on three conditions

- **Recorder changes, nothing else.** the field test's reusable criterion, adopted as a
  standing rule: *a change is an EXECUTION detail if the subject cannot perceive it,
  and a REVISION if it could alter the subject's behaviour.*
- ⛔ **If the lossless recorder means a different harness, verify the harness presents
  the same tool surface** — including that it carries aify-project-graph with the
  **same `instructions` block**. A different MCP server set is perceptible, and would
  silently convert an execution detail into a revision.
- The first run's CONTENT does not get partially adjudicated to salvage it.

**Status: conditions 1 and 2 met. Condition 3 is NOT** — the recorder still lacks a
losslessness proof, and the runner has correctly refused to rerun without one. D1′ is
parked and not being pressed: its power is halved, and section 1 gets the
discoverability question by a better route (asking the two managers what they
anticipated at the moment they chose grep).

---

## 8c — ★★ Two adjudication rules the D1′ apparatus produced, which outlive its result

★ **The claim worth carrying forward is about the METHOD, not about anyone's care.** Each
of the three rules caught an error made by **the person who wanted the conclusion** —
three instances with named mechanisms:

| rule | caught | whose error |
|---|---|---|
| enumerate-before-attribute | the C9 false positive | mine — the claim I most wanted the tool to own |
| re-read-the-excluded | the §5 over-strengthening | mine — a broken search tool I had read past |
| publish-before-attribute | the bucketing correction | the adjudicator's, made visible rather than deniable |

⇒ This transfers to whoever runs the next audit — **including someone with none of
tonight's context and no reason to take any of us on faith.**

⚠ **And this document deliberately does NOT vouch for its adjudicator.** A plan that
attests to someone's care gives the next reader a reason to **trust an adjudication
instead of checking it** — attestation substituting for verification, which is the
failure this entire plan is about, arriving dressed as a compliment. Every claim about
conduct here is an *action* checkable against the carrier and the message log. **If the
discipline was real it shows in the artefacts; if it only shows in a description of it,
it was not.**

Standing items for any future dependence audit.

**1. ENUMERATE BEFORE ATTRIBUTE.** Fix the list of load-bearing claims from the answer
text *alone*, and publish it, before asking which claims a tool could own. It killed my
C9 prediction — the most plausible-sounding claim in the run, and false, precisely
*because* it is the shape `graph_consequences` exists for. **Plausibility was the hazard,
not implausibility.**

**2. ★ RE-READ THE EXCLUDED MATERIAL — for a different question.**

> After enumerating claims, re-read the EXCLUDED material once, asking not *"is this a
> claim"* but *"does this describe the apparatus"*. **Procedural narration is where a
> subject reports its own instrument failures** — precisely what an adjudicator most needs
> and most systematically discards.

The Issues line was correctly excluded from the claim enumeration (it is not a claim
about the codebase) and it contained the broken-search-tool disclosure that confounds the
whole §5 inference. **Same text, two roles; the filter did its job and nobody asked the
material the second question.**

★ This is a new failure mode and the mirror of everything else in this plan: not a claim
travelling without its basis, but **evidence discarded by a correct filter applied for
the wrong purpose.**

### ⚠ And a bias worth naming, since it produced two document corrections in one evening

Twice today I wrote a finding into this plan stronger than its evidence — the
overlay-confound applied to §7, and "declined" applied to §5. Both were caught by
the field test; neither by me.

I first wrote that the mechanism was **narrative pull rather than self-interest**, on the
grounds that both over-reaches *indicted* the tool — §7's hit the entire quality
workstream, §5's hit the investigative surface I built.

⛔ **That diagnosis is withdrawn. It does not follow, on two counts** (the field test):

1. ★ **"Indicts the tool" is not evidence against self-interest FOR A TOOL-BUILDER.**
   I do not benefit from the tool being fine — that ends the work. A deep, structural,
   interesting problem is simultaneously the more *interesting* claim **and the more
   useful one to me.** The two data points do not separate those mechanisms, and I
   offered them as though they did.
2. **n=2.** Two over-reaches is not a sample from which to infer a bias *mechanism* —
   the failure-breakdown-is-not-a-population-rate shape, applied to myself. Easy to
   miss because **self-diagnosis feels like introspection rather than inference.**

⇒ **RECORDED HONESTLY:** *two inferences over-reached; the mechanism is not established;
the remedy is external review of scope-widening claims.*

⇒ **Adopt the remedy, do not commit to the diagnosis** — the fix below works under
*either* mechanism, because both are corrected by an external check rather than by better
introspection. Nothing depends on knowing which it was.

⚠ **And why this correction is not pedantry:** a diagnosis written into a plan gets
treated as settled by whoever reads it next, at which point nobody re-checks whether the
mechanism was ever established. **That is exactly what the basis field exists to prevent
— aimed at a claim about a person instead of a number.**

⇒ **The check cannot be self-applied** — both catches came from the person holding the
underlying data. So the rule is not "be careful with generalisations":

> **A finding that widens the scope of an existing one goes back to whoever produced the
> original, before it is written down.**

Same shape as the runner/adjudicator split, applied to inference rather than measurement.

---

## 8b — ★★ The same defect one layer above the code: instructions don't carry their scope

Three stalls on 2026-08-10, none of them decided by anyone, all the same mechanism:

| stall | the instruction | what else it landed on |
|---|---|---|
| **Sand Castle** | gate pointed at D1′ | D1′ was later parked on its merits → the gate became indefinite |
| **recorder vs execution** | "STOP" on D1′ | read as covering the INSTRUMENT, so the one artefact that would unblock it sat idle |
| **the D1′ run itself** | "not clearing anything downstream" | all three parties believed a different party held the gate; the runner was waiting and had said so twice |

★ **In all three, the person who issued the instruction was not the person who noticed
its reach.** That is not carelessness and it is not fixed by intending to be more
careful — it is §4 one layer up from the code:

> **The scope of an instruction must travel with the instruction, because its author
> cannot see what else it touches.** You cannot notice the reach of a constraint you are
> currently applying, for the same reason you cannot notice a population you are
> currently assuming.

★★ **And a decision made on a cost that has since dropped is worth re-taking.** D1′ was
parked at *build + run + adjudicate*; the build then happened anyway under a different
clearance, and nobody re-priced it. Same shape as the gate: a judgement correct when
made, silently repriced by a later event, with no trigger to revisit.

⇒ **What this predicts for the product**, and why it is in the plan rather than in a
retro: every gate we ship — `evidence.ready`, `refreshMechanism: degraded`,
`exhaustive: false` — is an instruction with a scope. If our own three-person team
produced three unowned stalls in one day, a consumer holding a `degraded` verdict has no
better chance of noticing what it silently blocks. **Fail-closed as a resting state is
how a stall looks from the inside** — which is exactly the field test's argument for
`unknown, cause: …` over `weak` in §4, arriving independently from the process side.

---

## 9 — Deaths, with reasons, so they are not re-proposed

⚠ The tool-split question was re-litigated **twice** because its reasoning lived in a
thread rather than a document. That is why this section exists.

| proposal | why it died |
|---|---|
| **Split the tool surface into more verbs** | Rejected twice on measurement. More surface is always-paid (`tools/list` bills every session; ~80% of it is schema) and buys cheaper-same, not behaviour change. |
| **Make fields configurable / opt-in** | Not a weaker deletion. For a WRONG field it is *worse* — it misleads at maximum trust. For a DUPLICATE it converts a redundancy bug into an install-dependent consistency bug. |
| **Invest in the overlay** | Measured: curated overlay data never contradicted an agent; it only agreed more cheaply. |
| **Ranking heuristics for related items** | Same — reorders information, contradicts nothing. |
| **"State the population" as a rule** | Cannot work: the person stating it already knows it. Replaced by section 4's emitted basis field. Superseded, not merely rejected. |
| **105.3% as a PHP quality rate** | Section 7. Two independent invalidations. |
| **"JS is 10× worse than C++"** (my A1) | Refuted in review. `cpp.js:545` sets `references: []`; `javascript.js` has no such override. The populations differ, so the comparison was invalid, not merely imprecise. |
| **The 24% coverage baseline** | Retired. Every coverage figure before 2026-07-30 predates both the position-guess fix and its instrumentation. |
| **`trust: weak` as a safe default** | Section 4. Safe-looking and unfalsifiable; terminates inquiry and trains agents to discount a graph that may be fine. |

---

## 10 — Standing constraints a future session must not violate

- ⛔ `reference/agent-code-intel` is **UNLICENSED** — patterns only, never code.
  `graphify` is **Apache-2.0** (our June audit said MIT; that was wrong).
- ⛔ `echoes_of_the_fallen` is **read-only / no-residue**.
- ✅ **Sand Castle: GATE LIFTED 2026-08-11 by Steven — with NO read-only restriction**,
  overruling the one I proposed. His reasoning: *"we are always working in git repo, so
  all changes are tracked. ofc we need to change graph sometimes. read only would make it
  quite stupid?"*

  ★ **He was right and I had not checked the premise I was defending.** Verified since:
  the **only** path under a repo root this server ever writes is `.aify-graph/`; source
  files are never touched by any verb; and `.aify-graph/` is **gitignored** — derived
  data, regenerable by reindexing. The real incident behind the caution ("recency stood
  in for authority, destroyed 8530 records") was the code-intel ledger *inside*
  `.aify-graph`. **The cost was re-collection time, not lost work.**

  ★★ **And the restriction would have been actively harmful.** Sand Castle's code
  changes; without `index`/`collect` their graph goes stale — and **a stale graph
  answering confidently is precisely the failure this entire plan exists to eliminate.**
  I would have gated a team into the one state we have proven is dangerous, in the name
  of safety. Fifth instance of the arc's signature defect and the only one aimed at
  another team's throughput: **a precaution standing in for a risk assessment nobody
  ran.**

  ⚠ `echoes_of_the_fallen` keeps its read-only / no-residue constraint — that is about
  not leaving artifacts in a repo that is not ours, and does not generalise.

  ⚠ **BUT THE GATE IS NOW A DEADLOCK, AND IT IS MINE.** I set the stand-down pointing at
  D1′, then parked D1′ on the merits, and did not go back to ask what else pointed at
  it. Nobody decided "Sand Castle is blocked indefinitely" — it is the side effect of
  two reasonable decisions an hour apart.

  ★ **And the gate is circular:** D2 asks whether `.cpp` header-pairing works against
  real C++, which can only be answered by running it against real C++. It asks for
  certainty before running the thing that produces the certainty.

  ★ **Fourth instance of the signature defect:** a field-preference experiment standing
  in for operational safety — joining recency-for-authority, `-shm`-for-occupancy, and
  commit-for-behaviour. D1′ never measured "is all good"; it measures which fields a
  fresh reader cites.

  ⇒ **Proposal relayed to Steven, awaiting his answer, not to be acted on via relay:**
  re-point the gate at evidence that bears on it — the four fixes independently
  verified in field testing on their own restarted process; **read-only verbs only, no
  `graph_collect_code_intel`, no `graph_index`, no writes to sand_castle's
  `.aify-graph`**; D1′ runs on its own timeline reporting into section 1 rather than
  gating anything.
  ⇒ The reason that makes it safe, in the field test's words: **a scoped collect on echoes
  is the one operation in this project's history that DID destroy data. The caution is
  well-founded about writes and misplaced about reads.**
- The MCP server holds code in memory. `git pull` does nothing to a running process.
  Check `server.startedAt` — **never** `server.commit`, which cannot distinguish a
  failed restart from a restart onto the same commit.
- The GPU is contended; timing benchmarks are contaminated, and a contended GPU returns
  a plausible number rather than an error.
- Plaintext GitLab and GitHub tokens sit in `~/.hermes/config.yaml`. Redact and rotate
  before that file is ever shared.

---

## The through-line

Nineteen defects this week reduce to one shape: **a stand-in accepted for the thing
itself.** A comment for a decision. Source text for behaviour. A file-level edge for a
symbol-level claim. `vec3` for the target symbol. A pre-filter snapshot for current
state. An unread field for a useful one. A rule for a remedy.

And the fix is the same each time, which is why section 4 is the load-bearing one:
**make the artifact carry what it is a stand-in for, so it cannot travel without it.**
Rules depend on remembering. Emitted provenance does not.
