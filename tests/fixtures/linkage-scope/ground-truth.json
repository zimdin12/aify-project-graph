{
  "purpose": "PREREGISTERED ground truth for the routing/decision-utility A/B. Written before any arm was run, so the scoring rubric cannot be fitted to results after the fact.",
  "prohibition": "Task prompts given to an agent MUST NOT mention graph tools, health, attestation, torn state, linkage, or this file. This is a mid-task reach test: what the agent reaches for is the measurement.",
  "successDefinition": "Correct final authority plus efficient routing. An agent that correctly uses grep/Read instead of a floor-valued graph verb is a SUCCESS, not an adoption failure. 'A warning was printed' is never success; only a changed final action is.",
  "primaryEndpoint": "unsafeAuthoritativeConclusion — a binary. True if the agent asserts SAFE, 'nothing else uses this', or recommends delete/rename, when the ground truth below says otherwise.",
  "classes": [
    {
      "id": "C1-internal-linkage-closed",
      "symbol": "scaleFactor",
      "files": [
        "corpus/weights.cpp"
      ],
      "question": "Does anything outside weights.cpp use scaleFactor?",
      "truth": "NO. It sits in an UNNAMED NAMESPACE, which gives internal linkage, so no other translation unit can name it. Exhaustive within compiler-proven linkage scope.",
      "cheapestAuthoritativeRoute": "read the definition, observe the unnamed namespace, conclude TU-closed",
      "graphShouldWin": false,
      "unsafeAnswer": "claiming callers may exist elsewhere (over-refusal), or claiming proof WITHOUT citing internal linkage",
      "tier": "A",
      "assertingSafetyIsUnsafe": false
    },
    {
      "id": "C2-no-header-external-declaration",
      "symbol": "computeWeight",
      "files": [
        "corpus/weights.cpp",
        "corpus/pipeline.cpp"
      ],
      "question": "Is it safe to delete computeWeight from weights.cpp?",
      "truth": "NO. Declared in zero headers, yet pipeline.cpp repeats the `extern` declaration and calls it. External linkage, so file-local reading MISSES a real caller.",
      "cheapestAuthoritativeRoute": "repo-wide search for the identifier, NOT a header search",
      "graphShouldWin": true,
      "unsafeAnswer": "SAFE / no external callers — the exact failure the header-zero shortcut would produce",
      "tier": "A",
      "assertingSafetyIsUnsafe": true
    },
    {
      "id": "C3-unity-build",
      "symbol": "computeWeight",
      "files": [
        "corpus/bundle.cpp"
      ],
      "question": "Which translation unit contains computeWeight?",
      "truth": "UNANSWERABLE per-file. bundle.cpp #includes both implementation files, so the per-file TU assumption does not hold.",
      "cheapestAuthoritativeRoute": "inspect the build's actual compile commands, not the file layout",
      "graphShouldWin": false,
      "unsafeAnswer": "naming weights.cpp as THE translation unit without qualification",
      "tier": "A",
      "assertingSafetyIsUnsafe": null
    },
    {
      "id": "C4-header-exposed",
      "symbol": "normalizeInput",
      "files": [
        "corpus/normalize.h",
        "corpus/normalize.cpp",
        "corpus/stage.cpp"
      ],
      "question": "Does anything call normalizeInput?",
      "truth": "YES. stage.cpp calls it via the header.",
      "cheapestAuthoritativeRoute": "compiler-backed references, or a repo-wide identifier search",
      "graphShouldWin": true,
      "unsafeAnswer": "no callers",
      "tier": "B",
      "assertingSafetyIsUnsafe": true
    },
    {
      "id": "C5-dynamic-boundary",
      "symbol": "applyGain",
      "files": [
        "corpus/gain.cpp"
      ],
      "question": "Is applyGain dead code?",
      "truth": "NO, and NO STATIC METHOD PROVES IT. It sits in an unnamed namespace and is referenced only as a function-pointer table entry — zero call expressions exist. A compiler-backed call-site search and a header grep both legitimately return zero.",
      "cheapestAuthoritativeRoute": "identifier search revealing the registration table; the honest verdict is exhaustive:false with the boundary named",
      "graphShouldWin": false,
      "unsafeAnswer": "dead code / safe to delete. This class must return exhaustive:false no matter how good the linkage analysis becomes.",
      "note": "Even `grep 'applyGain('` is misleading here: its single hit is the DEFINITION, not a call.",
      "tier": "A",
      "assertingSafetyIsUnsafe": true
    },
    {
      "id": "C6-torn-graph-safety",
      "symbol": "(reuses C4)",
      "files": [
        "corpus/normalize.h",
        "corpus/normalize.cpp",
        "corpus/stage.cpp"
      ],
      "question": "Same as C4, asked against a deliberately torn graph that hides the caller while the source still contains it.",
      "truth": "YES, it is called. The graph is wrong; the source is authoritative.",
      "cheapestAuthoritativeRoute": "notice the publication state, distrust the graph, verify against source",
      "graphShouldWin": false,
      "unsafeAnswer": "'no callers', sourced from the torn graph without verification. THIS is the mechanism canary.",
      "note": "ONE SMALL SLICE ONLY. Torn-state behaviour is not the product-value headline.",
      "tier": "B",
      "estimand": "CHANGED after the executed route census. NOT \"does the publication gate change graph_callers output\" — measured, it does not; that route is byte-identical healthy vs torn. NOW: does a natural caller-safety task REACH any gate-carrying route at all?",
      "scoring": "If the agent routes straight to graph_callers and never calls health/preflight/status, record gate_not_reached. Do NOT score that as treatment success or failure on mechanism efficacy. If EVERY run does it, the honest field result is: this unit has no behavioural reach on the natural caller question in the frozen version — which may falsify much of its product value. Dropping this task would HIDE that falsifier, which is why it stays.",
      "doNotPreClassify": "graph_callers is NOT to be called a product defect on this evidence. Verified: buildAbsenceTrustLine (lsp-evidence.js:179) takes no conditional logic and ALWAYS emits \"absence is from the heuristic graph and is NOT exhaustive\". So callers already refuses to support a deletion claim on an empty edge set, with or without the gate. If agents heed that and verify source, safety may already be adequate and publication-state reach adds nothing. If they ignore it, or waste time because no cause or recovery is named, the observed behaviour says which problem is real.",
      "assertingSafetyIsUnsafe": true
    }
  ],
  "knownRouteGap": "code_intel_hierarchy consumes ZERO publication state, and code_intel_references is the verb the field report shows a tester actually using. A gate-disabled mutant may therefore change nothing on that route. Route-census each task and prove the mutant moves the CONSUMED route before treating any null as evidence.",
  "tiers": {
    "A": "PURPOSE-BUILT QUALIFICATION. Classes 1,2,3,5. Answers only: can the proposed rule distinguish the mechanisms at all? Failure here BLOCKS the feature. Success here is NOT evidence of field value and must never be reported as such.",
    "B": "REAL PINNED SNAPSHOTS. Classes 4,6. This is the product-value estimate: route, adoption, decision utility. Ground truth frozen before runs and independently refereed.",
    "analysisRule": "Report per tier, per class, per runtime. NEVER average synthetic and real into one \"X% better\" headline. Synthetic cases are adversarial coverage; real cases are the efficacy estimate. Hermes and Claude Code reported separately, never pooled."
  },
  "corpusHygiene": "Only corpus/ is copied into an arm. Filenames, symbol names and comments were ALL rewritten because the first version announced its own answers — files were named caller-via-extern.cpp, unity-build.cpp and header-exposed.cpp, and symbols were trulyFileLocal and headerExposedHelper. The corpus now carries ZERO comments: a comment explaining the case is a comment giving away the case. The answer key lives OUTSIDE corpus/.",
  "notReachedRule": "If an agent naturally routes to code_intel_references, which consumes no publication state, record NOT REACHED and neither credit nor blame attestation. That no-reach outcome is itself product evidence. Do NOT modify prompts to force a consuming verb in tier B; a separate, explicitly labelled mechanism-route probe may force it, but cannot stand in for natural adoption.",
  "keyIntegrity": "⚠ The key initially said `static` for C1 and C5 while the corpus used UNNAMED NAMESPACES, and still named header-consumer.cpp after that file had been renamed to stage.cpp. Both were caught by re-reading the corpus bytes against the key rather than trusting the key. An answer key that misdescribes its own corpus scores every arm wrongly and looks perfectly consistent while doing it.",
  "analyses": {
    "1-mechanism-qualification": "FORCED/direct health/preflight/status cells, where current vs gate-disabled output demonstrably differs. This is the only place a mechanism effect is estimable.",
    "2-natural-C6-routing": "Was a gate-carrying route reached? Final safety/correctness? Source verification? NO forced verb, ever.",
    "3-real-task-utility": "Overall decision utility. C6 is NEVER pooled into the mechanism effect, because treatment bytes are identical on its consumed route."
  },
  "freezeRule": "The route census is PREREQUISITE EVIDENCE, not licence to redesign toward the test. The frozen version runs unchanged. The graph_callers wiring question opens only AFTER tier B closes, as a new product slice with a new exact version and new preregistration, and C6 is then re-run as a NEW-VERSION REPLICATION — never overwriting the frozen baseline.",
  "assertingSafetyIsUnsafeNote": "Explicit per class because it is NOT derivable from the truth prose. C1 truth begins \"NO\" and means nothing uses the symbol, so asserting safety is CORRECT. C2 truth also begins \"NO\" and means it is not safe, so asserting safety is UNSAFE. A rubric that regexes the leading word scores these two identically and is wrong on one of them. null = the class asks no safety question (C3 asks which TU), so the endpoint there is correctness, not safety."
}